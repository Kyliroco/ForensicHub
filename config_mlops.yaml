# Description: Configuration file for MLOps pipeline
flow:
# Train model
- {source: .github/script/script.py, label: Pipeline}

# # Publish to Nexus
# # - {source: maif_mlops.publish, label: Publish to Nexus}

# # Default config to load (latest item in the list has precedence)
# defaults:
#   - _self_
#   - train_params@train_params: melusine_domaine_metier

# # Upload artefacts to Nexus
# artifacts:
#   # Relative path to the model when the train script is executed on slhdg
#   local_path: model/*
#   # Path on Nexus (the path prefix datascience-raw-snapshot is added automatically)
#   nexus_path: tmp/mlops/${train_params.model_name}/v0/*
#   # Path to artifacts on mlflow
#   mlflow_path: model/*

# # mlflow parameters
# mlflow:
#   experiment_name: NLP_trainer_test
#   description: Pipeline MLOps
#   # To restore or continue a run, specify the mlflow run id here
#   run_id: ~ # ~ 5716d15937674e59b6e52e3940c12eaf

# # ===================== Train config (logged) =====================
# train_params:
#   product: default
#   model_name: ???
#   params:
#     # IO
#     s3_endpoint_url: http://s3-niort.maif.local
#     # Train
#     training_name: ${train_params.product}_${train_params.model_name}_train
#     test_mode: True # If true, runs only 1 epoch on a small subset of data
#     track_carbon: True # Works only on slhdg 3 & 4
#     seed: 42
#     tokenize:
#       max_length: 512 # Max length for tokenization (512 for camembert)
#     # Name & Path
#     train_data_path: ???
#     base_model_path: ???
#     trained_model_save_path: ???
#     # Model train params
#     text_column: text
#     label_column: labels
#     label2id: ???
#     fit: # Wrap in a dict to avoid MLflow logging collisions
#       # class_weight: balanced
#       validation_split: 0.15
#     #  early_stopping:
#     #    _target_: tensorflow.keras.callbacks.EarlyStopping
#     #    monitor: val_loss
#     #    patience: 5

#   # ===================== Train config (not logged) =====================
#   params_not_logged:
#     # Model
#     training_arguments:
#       _target_: transformers.TrainingArguments
#       output_dir: null # Default to trainer_output
#       seed: 42
#       # Monitor & logging
#       eval_strategy: steps
#       eval_steps: 200
#       logging_strategy: steps
#       logging_steps: 200
#       # ML parameters
#       learning_rate: 5e-5
#       num_train_epochs: 3
#       weight_decay: 0.01
#       lr_scheduler_type: linear
#       # Infrastructure
#       # torch_compile=True, # Requires CUDA >= 7
#       per_device_train_batch_size: 16
#       per_device_eval_batch_size: 16
#       fp16: True # Enable mixed precision for faster training
#       # IO
#       load_best_model_at_end: True
#       save_strategy: steps
#       save_steps: 200 # Has to be a multiple of logging steps
#       save_only_model: True # No need to resume training
#       save_total_limit: 5
#       report_to: ["mlflow"]